using System;
using System.Web;
using Base.Services;

namespace BaseWeb.Services
{
    public class SessionExtension
    {
        /*
        public static HttpCookie Item(string key, string value, int hours = 1)
        {
            HttpCookie item = new HttpCookie(key);
            item.Expires = DateTime.Now.AddHours(hours);  //available time
            item.Value = value;
            item.HttpOnly = true;
            if (_Config.GetBool(_Fun.SSL))
                item.Secure = true;
            return item;
        }
        */

    } //class
} //namespace
