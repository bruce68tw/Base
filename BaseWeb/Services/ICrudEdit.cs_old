using Base.Models;
using Base.Services;
using Newtonsoft.Json.Linq;

namespace BaseWeb.Services
{
    public interface ICrudEdit
    {
        //JObject GetPage(string ctrl, DtDto dt);

        CrudEdit EditService(string ctrl);

    }
}
